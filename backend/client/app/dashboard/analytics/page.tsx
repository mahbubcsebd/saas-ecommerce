import SiteAnalyticsClient from './client';

export const metadata = {
  title: 'Site Analytics | Mahbub Shop',
  description: 'Real-time visitor tracking and conversion analytics.',
};

export default function SiteAnalyticsPage() {
  return <SiteAnalyticsClient />;
}
